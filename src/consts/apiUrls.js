const API_URLS = {
  auth: '/auth/',
  users: '/users/',
  pockets: '/pockets/',
  categories: '/pockets/categories/',
  transactions: '/pockets/transactions/',
  targets: '/targets/',
};

export default API_URLS;
